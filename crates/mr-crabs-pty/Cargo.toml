[package]
name = "mr-crabs-pty"
version.workspace = true
edition.workspace = true
rust-version.workspace = true
license.workspace = true
publish.workspace = true

[features]
phase-timing = []

[dependencies]
libc = "0.2"
rustix = { version = "1.1.4", features = ["pty", "termios", "process", "fs", "std"] }

[dev-dependencies]
parking_lot.workspace = true
